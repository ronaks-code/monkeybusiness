# OpenAI API Configuration (Required)
OPENAI_API_KEY=your_openai_api_key_here
# For puzzle generation: gpt-5-nano (fastest/cheapest), gpt-5-mini, or gpt-4o-mini
OPENAI_MODEL=gpt-5-nano
# gpt-5-nano only supports temperature=1; the code forces 1.0 for that model. This value is used for others.
OPENAI_TEMPERATURE=0.7

# Output Configuration
OUTPUT_DIR=./output

# TikTok Content Posting API (Optional - only needed for --post-to-tiktok)
TIKTOK_ACCESS_TOKEN=your_tiktok_access_token_here
TIKTOK_PRIVACY_LEVEL=SELF_ONLY

# Google Drive upload (Optional - only needed for --upload-to-drive)
# Create a folder in Drive, open it, copy the ID from the URL: .../folders/FOLDER_ID
# Share that folder with your TikTok account holder so they can download and post.
GOOGLE_DRIVE_FOLDER_ID=

# Logging
LOG_LEVEL=INFO
